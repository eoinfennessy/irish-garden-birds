{% set navPages = collections.all | eleventyNavigation("Home") %}

<nav>
  <ul id="nav-list">
  {%- for entry in navPages %}
    <li{% if entry.url in page.url %} class="active"{% endif %}>
      <a href="{{ entry.url | url }}">{{ entry.title }}</a>
    </li>
  {%- endfor %}
  </ul>
</nav>